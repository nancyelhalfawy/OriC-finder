// Generated by CoffeeScript 1.9.0
(function() {
  var initiate;

  self.addEventListener('message', function(message) {
    return initiate(message.data);
  });

  initiate = function(input) {
    var dna, i, k, t, _i, _ref, _results;
    self.input = input;
    k = self.k = input.k;
    t = self.t = input.mutation_threshold;
    dna = input.DNA.lines.join('');
    self.postMessage({
      message: 'traverse-init',
      data: {
        input: input
      }
    });
    _results = [];
    for (i = _i = 0, _ref = dna.length - k; 0 <= _ref ? _i <= _ref : _i >= _ref; i = 0 <= _ref ? ++_i : --_i) {
      _results.push(self.postMessage({
        message: 'traverse',
        data: {
          sequence: dna.substr(i, k),
          index: i,
          threshold: t
        }
      }));
    }
    return _results;
  };

}).call(this);

//# sourceMappingURL=traverser.js.map
